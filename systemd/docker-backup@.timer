[Unit]
Description=docker-backup timer for stack %i

[Timer]
# The actual schedule comes from the drop-in:
#   /etc/systemd/system/docker-backup@%i.timer.d/schedule.conf
# Catch-up runs if the server was off at the scheduled time:
Persistent=true

[Install]
WantedBy=timers.target
