{
  "template_schema_version": 1,
  "name": "supabase",
  "description": "Supabase (self-hosted): uses the built-in DB auto-detection with the Supabase flavor (superuser dump including roles/globals + internal _supabase DB). No shell hooks needed. Only sets a sensible schedule and excludes large, regenerable caches.",
  "match": {
    "image_tokens": ["supabase/postgres"]
  },
  "db_autodetect": true,
  "schedule": "daily 02:00",
  "retention": {"daily": 7, "weekly": 4, "monthly": 6},
  "exclude_patterns": [
    "volumes/storage/tmp",
    "volumes/logs"
  ]
}
